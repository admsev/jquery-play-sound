= jquery playSound

This plugin defines $.playSound function.

  $.playSound('http://example.org/sound');

Do not add file extension like `.mp3` or `.ogg` - browser dows it automagically.

That's all folks!
